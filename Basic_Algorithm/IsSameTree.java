//public class IsSameTree {
/**
 * 判断两棵树是否是同一棵树
 * 中间的使用 return p == q 使用的很好。
 */

//    public boolean isSameTree(TreeNode p, TreeNode q) {
//        if (p == null || q == null)
//            return p == q; // 必须都是 null
//        return p.val == q.val && isSameTree(p.left, q.left) && isSameTree(p.right, q.right);
//    }
//}
